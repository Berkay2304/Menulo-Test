import 'package:flutter/material.dart';

ValueNotifier selectedIndexNotifier = ValueNotifier(0);
ValueNotifier selectedPageNotifier = ValueNotifier(0);

ValueNotifier currentOwnerPage = ValueNotifier(0);
